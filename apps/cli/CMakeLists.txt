include_directories(../../external/x_cargs/include)
add_executable(cli
    main.cpp
)
add_dependencies(cli
    generator
    parser
    tokenizer
    core
    cargs
)
target_link_libraries(cli
    generator
    parser
    tokenizer
    core
)
