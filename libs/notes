tokenizer -> parser -> generator



syntax:


@lua
@json
@arbitrary(asd=bsd, csd, dsd=asd)
@arbitrary2(asd)
struct MyType {
    @noLua
    // comment
    s32 id;
    @jsonAlias(Name)
    @jsonAlias(NAME)
    str name;
    // how to do functions?
    MyType SetNameAndReturnThis(str newName);
}
